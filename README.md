# Hardware and Software Hacks
### Camera
1. Change camera port names 
2. Change camera index

### To be able to load pre-trained model: 
1. Janky hack catered to the error message: `device` and `use_amp` are unrecognized keys. Popping them out. 
